// import { createShift, getShiftsByUser } from "../controller/shiftController.js";
// import { protect, authorize } from "../middleware/authMiddleware.js";
// import { Router } from "express";

// const router = Router();

// router
//   .route("/shifts/")
//   .post(protect, authorize("Employee"), createAvailability);
// router.route("/shifts").get(protect, authorize("Admin"), getAvailabilityByUser);
// router.route("/").get(protect, authorize("Employee"), getShiftsByUser);

// export default router;
